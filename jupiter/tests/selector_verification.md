# Selector Verification — jupiter

Jupiter is a Solana program. Solana does not use function selectors (no ABI encoding).
Transaction construction is handled by Jupiter's API which returns pre-built unsigned transactions.
No selector verification needed.
