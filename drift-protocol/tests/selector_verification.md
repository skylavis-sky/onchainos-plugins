# Selector Verification — drift-protocol

Drift Protocol is a Solana program — no EVM function selectors apply.
Write operations are currently stubbed pending protocol relaunch after 2026-04-01 security incident.
Read operations use Drift REST APIs (DLOB, data API) — no transaction construction needed.
